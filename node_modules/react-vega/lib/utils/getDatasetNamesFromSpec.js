"use strict";

require("core-js/modules/es.array.map");

require("core-js/modules/es.function.name");

exports.__esModule = true;
exports.default = getDatasetNamesFromSpec;

function getDatasetNamesFromSpec(spec) {
  var data = spec.data;

  if (data) {
    if (Array.isArray(data)) {
      // Array of data
      return data.map(function (_ref) {
        var name = _ref.name;
        return name;
      });
    }

    if (typeof data.name === 'string') {
      // Single data
      return [data.name];
    }
  }

  return [];
}